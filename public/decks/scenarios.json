{
  "deck": {
    "id": "deck_scenarios",
    "name": "Real Scenarios & Triage",
    "description": "Cross-stack troubleshooting: Angular search debouncing, 5s latency investigation, frontend vs backend debate, and outage fallbacks.",
    "category": "scenarios",
    "color": "#EF4444",
    "iconName": "Sparkles",
    "isDefault": true,
    "totalCards": 8
  },
  "cards": [
    {
      "id": "card_gpt_dbg_03",
      "deckId": "deck_scenarios",
      "front": "A user saves a customer successfully, but returning to the list shows the old name for several minutes. Where could stale data come from, and how would you isolate the layer?",
      "back": "I would enumerate the possible copies: local component state, global or query cache, browser HTTP or service-worker cache, CDN or reverse proxy, API in-memory cache, Redis, a read replica, and a database projection. I would compare resource version, timestamp, cache headers, and trace information at each boundary rather than repeatedly refreshing.\n\n**Key Points:**\n- Enumerates and tests multiple cache or replica layers.\n- Uses authoritative versions and targeted invalidation.\n- Defines acceptable consistency from the user workflow.",
      "notes": "### Comprehensive Senior Answer\nI would enumerate the possible copies: local component state, global or query cache, browser HTTP or service-worker cache, CDN or reverse proxy, API in-memory cache, Redis, a read replica, and a database projection. I would compare resource version, timestamp, cache headers, and trace information at each boundary rather than repeatedly refreshing.\n\nThe mutation response should contain the authoritative updated representation or version. The frontend can update the matching detail and list entries and invalidate queries whose membership may change. Server caches need a documented invalidation or bounded staleness policy; replicas require understood read-after-write behavior. I would test a direct uncached API read and compare it with the UI path to narrow the fault. The permanent fix includes version-aware telemetry so support can identify which layer served the old value.\n\n### 💡 Senior Engine & Architecture Insight\n> Enumerates and tests multiple cache or replica layers.\n\n### 🎯 Probable Interviewer Follow-ups\n- **How could a service worker cause this even when the API cache is correct?**\n- **When is eventual consistency acceptable to the user?**\n\n### 🔍 Interview Evaluation Signals\n**Strong Signals (What interviewers look for):**\n✅ Enumerates and tests multiple cache or replica layers.\n✅ Uses authoritative versions and targeted invalidation.\n✅ Defines acceptable consistency from the user workflow.\n\n**Warning Signals (Pitfalls to avoid):**\n⚠️ Calls the save endpoint twice.\n⚠️ Disables all caching before identifying the stale layer.",
      "spokenTip": "I approach this by distinguishing the core boundary: Enumerates and tests multiple cache or replica layers.",
      "tags": [
        "scenarios",
        "Data Consistency / Debugging",
        "Candidate-Calibrated"
      ],
      "difficulty": "Senior",
      "createdAt": "2026-09-02T10:59:47.366Z",
      "updatedAt": "2026-09-02T10:59:47.366Z"
    },
    {
      "id": "card_gpt_dbg_04",
      "deckId": "deck_scenarios",
      "front": "Production contains duplicate orders even though the frontend disables the submit button. How would you determine the cause and prevent recurrence?",
      "back": "I would correlate duplicate records by user, payload, timestamps, request IDs, and upstream traces. Causes may include double events, browser or proxy retry, client timeout followed by retry, queue redelivery, multiple service instances, or a worker retry after an unknown commit result. The frontend button helps UX but cannot enforce uniqueness across these paths.\n\n**Key Points:**\n- Investigates every retry and redelivery point.\n- Uses server-side idempotency and database enforcement.\n- Tests concurrency and lost-response scenarios.",
      "notes": "### Comprehensive Senior Answer\nI would correlate duplicate records by user, payload, timestamps, request IDs, and upstream traces. Causes may include double events, browser or proxy retry, client timeout followed by retry, queue redelivery, multiple service instances, or a worker retry after an unknown commit result. The frontend button helps UX but cannot enforce uniqueness across these paths.\n\nI would define the logical operation and give it an idempotency key or business uniqueness constraint. The API records the key and outcome atomically with order creation, returning the same resource for a legitimate retry. Queue consumers must also be idempotent. If the domain has a natural uniqueness rule, enforce it in the database and translate conflicts safely. I would add metrics for duplicate-key hits and tests that send concurrent identical commands and simulate a lost response after commit.\n\n### 💡 Senior Engine & Architecture Insight\n> Investigates every retry and redelivery point.\n\n### 🎯 Probable Interviewer Follow-ups\n- **How would you distinguish a legitimate repeated purchase from a duplicate retry?**\n- **What if the external payment succeeds before the order transaction fails?**\n\n### 🔍 Interview Evaluation Signals\n**Strong Signals (What interviewers look for):**\n✅ Investigates every retry and redelivery point.\n✅ Uses server-side idempotency and database enforcement.\n✅ Tests concurrency and lost-response scenarios.\n\n**Warning Signals (Pitfalls to avoid):**\n⚠️ Adds a longer frontend debounce.\n⚠️ Checks for duplicates with a read followed by an unprotected insert.",
      "spokenTip": "I approach this by distinguishing the core boundary: Investigates every retry and redelivery point.",
      "tags": [
        "scenarios",
        "Reliability / Duplicate Side Effect",
        "Candidate-Calibrated"
      ],
      "difficulty": "Senior",
      "createdAt": "2026-09-02T10:59:47.366Z",
      "updatedAt": "2026-09-02T10:59:47.366Z"
    },
    {
      "id": "card_gpt_dbg_05",
      "deckId": "deck_scenarios",
      "front": "A list loads in 200 ms locally but takes 12 seconds in production only for some tenants. How would you approach the diagnosis?",
      "back": "The tenant pattern suggests data volume, distribution, permissions, or plan sensitivity. I would compare traces for a fast and slow tenant, including frontend wait, API duration, SQL command, row counts, execution plan, cache status, and dependency calls. I would reproduce with sanitized production-scale data or query parameters in a safe environment.\n\n**Key Points:**\n- Compares fast and slow traces and recognizes tenant data shape.\n- Inspects plans, rows, pagination, permissions, and query shape.\n- Validates against worst-case production-scale data.",
      "notes": "### Comprehensive Senior Answer\nThe tenant pattern suggests data volume, distribution, permissions, or plan sensitivity. I would compare traces for a fast and slow tenant, including frontend wait, API duration, SQL command, row counts, execution plan, cache status, and dependency calls. I would reproduce with sanitized production-scale data or query parameters in a safe environment.\n\nCommon causes include missing composite indexes for tenant plus filter and sort, deep offset pagination, N+1 access, a parameter-sensitive plan, a tenant-specific permission join, or returning too much data. I would not add a timeout or cache before understanding the query. The fix might be projection, keyset pagination, an index, statistics or plan remediation, or an API shape change. I would load-test the worst realistic tenant and add latency segmentation by tenant size without exposing tenant data in public metrics.\n\n### 💡 Senior Engine & Architecture Insight\n> Compares fast and slow traces and recognizes tenant data shape.\n\n### 🎯 Probable Interviewer Follow-ups\n- **How would you investigate a parameter-sensitive execution plan?**\n- **What frontend changes could reduce perceived delay while the backend fix is developed?**\n\n### 🔍 Interview Evaluation Signals\n**Strong Signals (What interviewers look for):**\n✅ Compares fast and slow traces and recognizes tenant data shape.\n✅ Inspects plans, rows, pagination, permissions, and query shape.\n✅ Validates against worst-case production-scale data.\n\n**Warning Signals (Pitfalls to avoid):**\n⚠️ Assumes production servers only need more CPU.\n⚠️ Caches the slow response without understanding tenant isolation.",
      "spokenTip": "I approach this by distinguishing the core boundary: Compares fast and slow traces and recognizes tenant data shape.",
      "tags": [
        "scenarios",
        "Performance / Production-Only Issue",
        "Candidate-Calibrated"
      ],
      "difficulty": "Senior",
      "createdAt": "2026-09-02T10:59:47.366Z",
      "updatedAt": "2026-09-02T10:59:47.366Z"
    },
    {
      "id": "card_gpt_dbg_07",
      "deckId": "deck_scenarios",
      "front": "File uploads succeed up to 10 MB and fail above that size with inconsistent errors. Which layers would you inspect?",
      "back": "I would inspect limits and timeouts at every hop: browser code, frontend development or production proxy, CDN or load balancer, ingress or web server, ASP.NET request limits, multipart parser, application validation, storage SDK, and any security scanner. The smallest limit wins, and different layers may return different statuses or close the connection.\n\n**Key Points:**\n- Checks every proxy, server, parser, and storage boundary.\n- Proposes scalable direct or resumable upload.\n- Includes validation, scanning, authorization, and UX.",
      "notes": "### Comprehensive Senior Answer\nI would inspect limits and timeouts at every hop: browser code, frontend development or production proxy, CDN or load balancer, ingress or web server, ASP.NET request limits, multipart parser, application validation, storage SDK, and any security scanner. The smallest limit wins, and different layers may return different statuses or close the connection.\n\nFor large files, I would prefer direct or resumable upload to object storage using short-lived authorized upload instructions, followed by a finalize command that records metadata and triggers scanning. That avoids buffering the whole file through the API. The server still validates size, type based on content rather than extension alone, tenant quota, and ownership. The UI needs progress, cancellation, retry, and clear terminal state. I would log safe size and stage metadata and test just below and above each configured limit.\n\n### 💡 Senior Engine & Architecture Insight\n> Checks every proxy, server, parser, and storage boundary.\n\n### 🎯 Probable Interviewer Follow-ups\n- **Why should the API avoid loading the entire file into memory?**\n- **How would you prevent an uploaded file from being downloadable before scanning?**\n\n### 🔍 Interview Evaluation Signals\n**Strong Signals (What interviewers look for):**\n✅ Checks every proxy, server, parser, and storage boundary.\n✅ Proposes scalable direct or resumable upload.\n✅ Includes validation, scanning, authorization, and UX.\n\n**Warning Signals (Pitfalls to avoid):**\n⚠️ Only increases the frontend file-size check.\n⚠️ Trusts the filename extension as the content type.",
      "spokenTip": "I approach this by distinguishing the core boundary: Checks every proxy, server, parser, and storage boundary.",
      "tags": [
        "scenarios",
        "Uploads / Infrastructure Debugging",
        "Candidate-Calibrated"
      ],
      "difficulty": "Strong Mid",
      "createdAt": "2026-09-02T10:59:47.366Z",
      "updatedAt": "2026-09-02T10:59:47.366Z"
    },
    {
      "id": "card_scen-angular-keystroke-flood",
      "deckId": "deck_scenarios",
      "front": "Scenario: An Angular search box is sending an HTTP request on every single keystroke. How would you investigate and fix it?",
      "back": "I would replace raw `(input)` event handlers with an RxJS reactive stream using `debounceTime(300)`, `distinctUntilChanged()`, `filter()`, and `switchMap()` to prevent flooding the backend and avoid out-of-order race conditions.\n\n**Key Points:**\n- Problem: Backend CPU overload + Frontend race conditions / out-of-order data\n- RxJS debounceTime(300) to pause before firing\n- RxJS distinctUntilChanged() to avoid redundant queries\n- RxJS switchMap() to automatically cancel stale in-flight HTTP requests\n- catchError placed inside inner observable to keep stream alive",
      "notes": "### Comprehensive Senior Answer\nIn an interview, I explain that typing \"laptop\" triggers 6 separate HTTP requests within 1 second if bound directly to an input change event. This causes two huge problems: 1) **Backend Overload**: Hundreds of useless database queries for partial queries like \"l\", \"la\", \"lap\", and 2) **Race Conditions**: If the network is jittery, the response for \"la\" might arrive *after* the response for \"laptop\", overwriting the search results with stale data! In Angular, I fix this using RxJS: I bind a `FormControl` or `Subject` to the search input, pipe through `debounceTime(300)` (waits 300ms after user stops typing), `distinctUntilChanged()` (ignores duplicate keystrokes like arrow keys), `filter(text => text.length >= 2)` (avoids 1-letter searches), and `switchMap(term => this.api.search(term))`. `switchMap` is the key operator here because it automatically cancels any pending in-flight HTTP request when a new search term arrives.\n\n### Code Example\n```typescript\n// Angular 18+ Reactive Search Component\nimport { Component, inject } from '@angular/core';\nimport { FormControl, ReactiveFormsModule } from '@angular/forms';\nimport { Observable, of } from 'rxjs';\nimport { debounceTime, distinctUntilChanged, filter, switchMap, catchError } from 'rxjs/operators';\nimport { AsyncPipe, NgFor } from '@angular/common';\n\n@Component({\n  selector: 'app-product-search',\n  standalone: true,\n  imports: [ReactiveFormsModule, AsyncPipe, NgFor],\n  template: `<input [formControl]=\"searchControl\" placeholder=\"Search products...\" />\n             <div *ngFor=\"let item of results$ | async\">{{ item.name }}</div>`\n})\nexport class ProductSearchComponent {\n  searchControl = new FormControl('');\n  \n  results$: Observable<Product[]> = this.searchControl.valueChanges.pipe(\n    debounceTime(300),                         // Wait 300ms pause\n    distinctUntilChanged(),                    // Only if value actually changed\n    filter(term => (term?.length ?? 0) >= 2),  // Minimum 2 characters\n    switchMap(term => this.productService.search(term!).pipe(\n      catchError(error => {\n        console.error(error);\n        return of([]);                         // Don't kill the stream on error!\n      })\n    ))\n  );\n}\n```\n_Clean RxJS pipeline using debounceTime and switchMap._\n\n### 💡 Senior Engine & Architecture Insight\n> Don't forget `catchError` inside the `switchMap` inner observable. If you place `catchError` on the outer pipe and an HTTP call fails, the entire Angular search stream completes and stops listening to future keystrokes permanently.\n\n### 🎯 Probable Interviewer Follow-ups\n- **Q: What is the difference between `switchMap`, `mergeMap`, and `concatMap` for this use case?**\n  *A:* `switchMap` cancels the previous HTTP request (perfect for search typeaheads). `mergeMap` runs all requests in parallel (creates race conditions). `concatMap` queues requests sequentially (causes severe lag for user typing).\n- **Q: How do you test this reactive search pipeline in Angular unit tests?**\n  *A:* Use `fakeAsync` and `tick(300)` with `HttpTestingController` to advance the virtual debounce clock and assert that only a single HTTP request was made.",
      "spokenTip": "I use an RxJS pipe with debounceTime(300) to wait for the user to pause, and switchMap to automatically cancel outdated pending requests.",
      "tags": [
        "Scenarios",
        "Angular",
        "RxJS",
        "Performance",
        "API"
      ],
      "difficulty": "Intermediate",
      "createdAt": "2026-09-02T10:59:47.366Z",
      "updatedAt": "2026-09-02T10:59:47.366Z"
    },
    {
      "id": "card_scen-api-5-second-latency-investigation",
      "deckId": "deck_scenarios",
      "front": "Scenario: An API endpoint normally takes 100ms, but intermittently takes 5 seconds to respond. How would you systematically investigate and resolve this?",
      "back": "I would check application logs with Correlation IDs, examine Application Performance Monitoring (APM / OpenTelemetry) waterfalls to isolate whether time is spent in SQL queries, external HTTP dependencies, or GC pauses, and check thread pool starvation.\n\n**Key Points:**\n- Top-down APM waterfall inspection (Datadog / Application Insights)\n- Check Database: lock contention, slow execution plans, connection pool exhaustion\n- Check External dependencies: missing timeouts on downstream HTTP calls\n- Check .NET ThreadPool starvation caused by sync-over-async (.Result/.Wait)\n- Check for container cold starts or GC pauses",
      "notes": "### Comprehensive Senior Answer\nWhen investigating intermittent 5-second latency, I follow a top-down diagnostic approach: 1) **Network & Gateway**: Look at APM traces (Datadog/AppInsights/Jaeger) to see the end-to-end waterfall breakdown. Is the 5 seconds spent waiting on the network, Kestrel pipeline, database, or external microservice? 2) **Database Layer**: If database time is high, check for SQL Server lock waits, missing indexes, or connection pool exhaustion (default timeout is often 5-15 seconds when all pooled connections are busy). 3) **Downstream HTTP Calls**: Check if the backend is calling an external third-party API (e.g. payment/shipping) without a proper timeout. 4) **.NET Runtime & Threading**: Check for **Thread Pool Starvation** caused by someone calling `.Result` or `.Wait()` on async tasks, or long Garbage Collection (GC) pauses under high memory pressure. 5) **Cold Starts**: If running in serverless / Kubernetes scale-to-zero, verify if it's a container cold start.\n\n### Code Example\n```text\nDiagnostic Checklist:\n1. APM / Distributed Trace Waterfall:\n   └── [API Gateway] 5050ms\n       └── [ASP.NET Core] 5020ms\n           ├── [EF Core SqlConnection.OpenAsync()] 4980ms  <-- BOTTLENECK! (Connection Pool Starvation)\n           └── [SELECT * FROM Users] 12ms\n\nConclusion: DB query took only 12ms, but waiting to GET a connection from the pool took 5 seconds!\n```\n_APM waterfall revealing connection pool exhaustion rather than query execution time._\n\n### 💡 Senior Engine & Architecture Insight\n> A classic cause of 5-second spikes in .NET is the ThreadPool injection rate. If worker threads are blocked by synchronous calls (`.Result`), the .NET ThreadPool only creates 1 or 2 new threads per second, taking several seconds to recover.\n\n### 🎯 Probable Interviewer Follow-ups\n- **Q: How do you detect connection pool leaks in EF Core?**\n  *A:* Check if DbContext is inadvertently registered as Singleton or if raw ADO.NET `SqlConnection` objects are created without a `using` statement or proper async disposal.\n- **Q: How do you configure HTTP timeouts using Polly or HttpClient in .NET?**\n  *A:* Configure `HttpClient.Timeout = TimeSpan.FromSeconds(3)` or apply a Polly `TimeoutPolicy` so slow downstream dependencies fail fast rather than hanging web threads.",
      "spokenTip": "I use APM waterfall traces to instantly isolate whether the 5 seconds is spent in the database connection pool, an external API, or thread pool starvation.",
      "tags": [
        "Scenarios",
        "Troubleshooting",
        "Performance",
        "ASP.NET Core",
        "APM"
      ],
      "difficulty": "Strong Mid",
      "createdAt": "2026-09-02T10:59:47.366Z",
      "updatedAt": "2026-09-02T10:59:47.366Z"
    },
    {
      "id": "card_scen-frontend-vs-backend-latency-debate",
      "deckId": "deck_scenarios",
      "front": "Scenario: The Frontend team says \"The API is slow (3 seconds)\", while the Backend team says \"The API logs show it took only 40ms\". How do you investigate and resolve this disagreement?",
      "back": "I would analyze the complete browser Network Timing breakdown (DNS, TLS, TTFB, Content Download), inspect intermediate hops (WAF, CDN, API Gateway), and correlate frontend requests with backend server logs using a shared Correlation ID.\n\n**Key Points:**\n- Understand both perspectives: Browser Total Time vs Server Processing Time\n- Analyze browser Timing tab: DNS, TLS, Stalled, TTFB, Content Download\n- Identify payload size issues (missing Brotli/Gzip or returning unpaginated arrays)\n- Check browser 6-connection bottleneck (HTTP/1.1 vs HTTP/2)\n- Check un-cached CORS preflights (Access-Control-Max-Age)\n- Correlate via Traceparent / X-Correlation-ID across gateway logs",
      "notes": "### Comprehensive Senior Answer\nAs a full-stack engineer, I bridge this exact gap all the time! Both teams might actually be telling the truth. The backend measures execution time inside the controller (40ms), while the browser measures Total Request-Response Time (3000ms). Here is how I diagnose it: 1) **Browser DevTools Network Tab**: Inspect the **Timing breakdown**: Is the delay in **DNS Lookup**, **Initial Connection (TLS Handshake)**, **TTFB (Time to First Byte)**, or **Content Download**? 2) **Payload Size**: If TTFB is 50ms but Content Download is 2.9 seconds, the backend returned a massive 15MB uncompressed JSON payload. 3) **Intermediate Hops**: If TTFB is 2.9 seconds, the delay is happening in the API Gateway, Cloudflare WAF, or reverse proxy queue before hitting Kestrel. 4) **CORS Preflight**: Check if an un-cached OPTIONS preflight request is taking time. 5) **Correlation ID**: Match the frontend request timestamp and `X-Correlation-ID` against the API Gateway access logs.\n\n### Code Example\n```text\nBrowser DevTools Timing Breakdown Analysis:\n- Queueing / Stalled: 1200ms  --> Browser hit the 6-connection-per-domain limit (HTTP/1.1)\n- DNS Lookup: 20ms\n- Initial Connection (TLS): 80ms\n- Request Sent: 2ms\n- Waiting for server response (TTFB): 60ms  --> Backend is indeed fast!\n- Content Download: 1700ms   --> 12MB JSON payload! Needs Gzip/Brotli compression + pagination!\n```\n_DevTools timing exposes that connection queuing and payload size caused the 3s delay._\n\n### 💡 Senior Engine & Architecture Insight\n> Common hidden culprits: 1) Browser connection limits (HTTP/1.1 caps at 6 connections per domain; solve by enabling HTTP/2 multiplexing), 2) Missing Gzip/Brotli response compression, 3) SSL/TLS negotiation latency on slow mobile networks, 4) Un-cached CORS preflight requests on every call.\n\n### 🎯 Probable Interviewer Follow-ups\n- **Q: How do you cache CORS preflight requests?**\n  *A:* Set the `Access-Control-Max-Age: 86400` header in the backend CORS policy so browsers don't repeat OPTIONS preflights for 24 hours.\n- **Q: Why does enabling HTTP/2 resolve browser connection queueing?**\n  *A:* HTTP/2 multiplexes hundreds of concurrent requests over a single TCP connection, eliminating the browser's 6-connection-per-domain queueing stall in HTTP/1.1.",
      "spokenTip": "I open browser DevTools to check whether the delay is in TTFB, TLS handshake, or Content Download, and correlate it with the backend via Correlation ID.",
      "tags": [
        "Scenarios",
        "Debugging",
        "Frontend",
        "Backend",
        "Networking",
        "DevTools"
      ],
      "difficulty": "Strong Mid",
      "createdAt": "2026-09-02T10:59:47.366Z",
      "updatedAt": "2026-09-02T10:59:47.366Z"
    },
    {
      "id": "card_scen-microservice-payment-unavailable-fallback",
      "deckId": "deck_scenarios",
      "front": "Scenario: Order Service needs to call Payment Service, but Payment Service is temporarily down (503 Service Unavailable). How do you handle this gracefully?",
      "back": "I would use a Circuit Breaker to prevent thread exhaustion, queue the payment command asynchronously in RabbitMQ, store the order in `PendingPayment` status, and notify the user that their order is being processed.\n\n**Key Points:**\n- Circuit Breaker (Polly) fails fast to prevent thread pool exhaustion\n- Asynchronous fallback: save order as Pending and push command to Message Queue\n- Return 202 Accepted with clear user feedback in UI\n- Background consumers process queue when Payment Service recovers\n- Dead Letter Queue (DLQ) and compensating transactions for permanent failures",
      "notes": "### Comprehensive Senior Answer\nIn a well-architected distributed system, a temporary outage in a downstream dependency should never crash the entire user checkout experience. Here is my strategy: 1) **Fast Failure via Circuit Breaker**: Polly detects that Payment Service is down and trips the circuit breaker open, failing fast in <5ms instead of waiting for a 30-second timeout. 2) **Asynchronous Fallback via Message Queue**: The Order Service writes the order to the database with status `PaymentPending` and pushes a `ProcessPaymentCommand` message to a durable RabbitMQ / SQS queue. 3) **Client Response**: Return `202 Accepted` to the frontend with `{ orderId: \"123\", status: \"Processing\" }`. The Angular UI shows a friendly message: \"Your order is received and payment is processing in the background\". 4) **Queue Processing**: When Payment Service recovers, background consumer workers drain the queue and process the payments reliably without losing any orders.\n\n### Code Example\n```csharp\n// Graceful Fallback in Order Controller\ntry\n{\n    var paymentResult = await _paymentClient.ChargeAsync(orderDto);\n    await _orderService.CompleteOrderAsync(orderDto.Id, paymentResult);\n    return Ok(new { status = \"Completed\" });\n}\ncatch (BrokenCircuitException) // Circuit breaker open!\n{\n    // Fallback: Queue payment command for async processing\n    await _messageBus.PublishAsync(new ProcessDeferredPaymentCommand(orderDto));\n    await _orderService.MarkOrderPendingAsync(orderDto.Id);\n    \n    // Return 202 Accepted to UI\n    return Accepted(new { \n        status = \"Processing\", \n        message = \"Payment is processing in the background. We will email your confirmation shortly.\" \n    });\n}\n```\n_Handling downstream outage with Circuit Breaker and asynchronous queue fallback._\n\n### 💡 Senior Engine & Architecture Insight\n> Ensure you set up Dead Letter Queues (DLQ) with alerting. If a customer's card is permanently declined or has insufficient funds when processed from the queue, trigger an automated email to the user with a link to update their payment method.\n\n### 🎯 Probable Interviewer Follow-ups\n- **Q: What if inventory was reserved but payment fails permanently later?**\n  *A:* Execute a compensating transaction in the Saga to release the reserved inventory back to the warehouse.\n- **Q: How do you inform the frontend in real time when background payment finishes?**\n  *A:* Use SignalR WebSockets: when the background worker completes the payment, broadcast an event to the client's connection ID to update the UI from \"Processing\" to \"Confirmed\".",
      "spokenTip": "Instead of failing the customer checkout, we accept the order as \"PaymentPending\", push the payment command to a message queue, and process it when Payment Service recovers.",
      "tags": [
        "Scenarios",
        "Microservices",
        "Resilience",
        "Circuit Breaker",
        "RabbitMQ"
      ],
      "difficulty": "Strong Mid",
      "createdAt": "2026-09-02T10:59:47.366Z",
      "updatedAt": "2026-09-02T10:59:47.366Z"
    }
  ]
}